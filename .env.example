# Required: Linear API key from Settings → API → Personal API keys
LINEAR_API_KEY=lin_api_xxxxx

# Optional: Webhook signature secret for validating Linear webhooks
# Get this when you create a webhook in Linear (Settings → API → Webhooks)
LINEAR_WEBHOOK_SECRET=

# Optional: Polling interval in milliseconds (use if webhooks aren't available)
# Set to 0 to disable polling (webhook mode)
# Example: 30000 = check every 30 seconds
LINEAR_POLLING_INTERVAL_MS=0

# Optional: Path to tenants.json config file
# Defaults to ./tenants.json in project root
TENANTS_CONFIG_PATH=

# Optional: Default repository path for the runner (standalone mode)
DEFAULT_REPO_PATH=/path/to/your/repo

# Optional: GitHub token for PR creation
# Needs repo permissions
GITHUB_TOKEN=ghp_xxxxx

# Optional: Server port (default: 3000)
PORT=3000
