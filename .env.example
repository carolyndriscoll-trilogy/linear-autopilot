# Required: Linear API key from Settings → API → Personal API keys
LINEAR_API_KEY=lin_api_xxxxx

# Required: Linear team ID (found in team settings URL or via API)
LINEAR_TEAM_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Optional: Default repository path for the runner
DEFAULT_REPO_PATH=/path/to/your/repo

# Optional: GitHub token for future PR integration
GITHUB_TOKEN=ghp_xxxxx
